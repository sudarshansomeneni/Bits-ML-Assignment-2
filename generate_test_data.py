# Optional helper:
# train.py already creates test_data.csv automatically.
# This file is kept only as a simple entry point for students who prefer
# to regenerate the test data through a separate command.

from train import main

if __name__ == "__main__":
    main()
